# -*- mode: python; -*-

Import('env')

env = env.Clone()

env.Append(
    CPPDEFINES = [
        'NWORDS_BIGENDIAN'  # Use WORDS_BIGENDIAN on big endian architectures
    ],
    CPPPATH = [
        '../..'
    ],
)

input_files = [
    'modp_b64.cc',
    'modp_b64.h',
    'modp_b64_data.h',
]

target_files = env.StaticLibrary('modp_b64', input_files)
