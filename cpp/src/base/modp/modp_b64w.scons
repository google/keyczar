# -*- mode: python; -*-

Import('env')

env = env.Clone()

env.Append(
    CPPDEFINES = [
        'NWORDS_BIGENDIAN'  # Use WORDS_BIGENDIAN on big endian architectures
    ],
    CPPPATH = [
        '../..'
    ],
)

input_files = [
    'modp_b64w.cc',
    'modp_b64w.h',
    'modp_b64w_data.h',
]

target_files = env.StaticLibrary('modp_b64w', input_files)
